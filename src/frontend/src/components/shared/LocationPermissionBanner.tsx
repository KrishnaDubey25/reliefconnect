// Location permission banner removed — geolocation is requested silently in
// the background without showing any prompts or banners to the user.
// This file is kept to avoid breaking any existing imports.
export function LocationPermissionBanner() {
  return null;
}
